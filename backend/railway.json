{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "true",
    "watch": ["backend"]
  },
  "deploy": {
    "startCommand": "cd backend 2>/dev/null || true; node server.js",
    "restartPolicyType": "always"
  }
}
